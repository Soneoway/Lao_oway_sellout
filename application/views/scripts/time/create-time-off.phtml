<style>
   #staff_id{
        margin-left: 200px;
       margin-top: -8px;
   }
   input[type='text']{
       width: 350px;
   }
    select{
        width: 360px !important;
    }
    textarea{
        width: 330px !important;
    }
    .date{
        border-radius: 3px;
        width: 330px !important;
    }
</style>
<div class="page-header">
        <h1>Create time off for staff: </h1>
</div>

<?php
if (isset($this->messages) and $this->messages)
    foreach ($this->messages as $message):
        echo '<div class="alert alert-error">' . $message . '</div>';
    endforeach;
?>


<?php if (isset($this->WARNING_TIMING_MESSAGES) and $this->
WARNING_TIMING_MESSAGES): ?>
<div class="alert-msg">
    <p class="msg"><?php echo $this->WARNING_TIMING_MESSAGES; ?></p>
</div>
<?php endif; ?>


<div class="row-fluid">
    <?php if(isset($this->time) and $this->time) : ?>
    
    <div class="span6">
        <div class="control-group">
            <h3>Bạn đã chấm công hôm nay, vui lòng quay lại vào ngày mai !</h3>
        </div>
    </div>
    
    <?Php else : ?>
    
    <iframe name="ifrmSave" id="ifrmSave" width="100%" style="display:none;"></iframe>
    <form role="form" id="time_form" action="<?php echo HOST . 'time/create-time-off'; ?>" method="post"  class="row-fluid">
        <div class="span6">
               <div class="control-group">
                    <h3></h3>
               </div>
               <div class="control-group">
                   <label for="" class="span2">Staff</label>
                   <input type="text" value="" name="search_staff" id="search_staff" autocomplete="off" placeholder='Select a staff'/>
                   <select name="staff_id" id="staff_id" required="require" size="5">
                       <option value="">SELECT A STAFF</option>
                        <?php
                            if(isset($this->staffs) AND $this->staffs):
                                foreach($this->staffs as $item):
                            ?>
                                    <option value="<?php echo $item['id']?>" <?php echo (isset($this->rowChildbearing['staff_id']) AND $this->rowChildbearing['staff_id'] == $item['id']) ? 'selected': ''?>><?php echo $item['staff_name']?></option>
                        <?php
                                endforeach;
                            endif;
                        ?>
                   </select>
               </div>
               <div class="control-group">
                     <label for="type" class="span2">Type</label>
                     <select name="type" id="type" required="require" >
                         <option value="">Choose</option>
                         <?php
                            $arrOffType = unserialize(OFF_TYPE);
                            foreach($arrOffType as $key => $value):
                         ?>
                                <option value="<?php echo $key;?>"><?php echo $value;?></option>
                         <?php
                            endforeach;
                         ?>
                     </select>
               </div>
               
               <div class="control-group">
                     <label for="shift" class="span2">Shift</label>
                     <select name="shift" id="shift" required="require" >
                       <option value="">Choose</option>
                        <option value="0">Fullday</option>
                       <option value="1">1/2 day</option>
                     </select>
               </div>
               
                <div class="control-group">
                    <label for="from_date" class="span2">From Date</label>
                    <input required="require" autocomplete="off" type="text" class="form-control search-query date span2" id="from_date" name="from_date" value="<?php isset($this->rowChildbearing['from_date']) and printf($this->rowChildbearing['from_date']);?>">
                </div>

                <div class="control-group">
                    <label for="to_date" class="span2">To Date</label>
                    <input  autocomplete="off" type="text" class="form-control search-query date span2" id="to_date" name="to_date" value="<?php (isset($this->rowChildbearing['to_date'])  and $this->rowChildbearing['to_date'] and $this->rowChildbearing['to_date'] != '0000-00-00 00:00:00') ? printf($this->rowChildbearing['to_date']) : printf('') ;?>">
                </div>
                
                 <div class="control-group">
                    <label for="reason" class="span2">Reason</label>
                    <textarea  id="reason" name="reason" type="text" class="input-medium search-query form-control span2" cols="40" rows="5" ><?php (isset($this->rowChildbearing['reason']) AND $this->rowChildbearing['reason']) ? printf($this->rowChildbearing['reason']) : printf('') ;?></textarea>
                </div>

                <div class="control-group">
                    <input type="hidden" name="id" id="id" value="<?php if ( isset($this->rowChildbearing) and $this->rowChildbearing) echo $this->rowChildbearing->id;?>">
                    <input type="hidden" name="back_url" id="back_url" value="<?php if ( isset($this->back_url) and $this->back_url ) echo $this->back_url;?>">
                    <button id="btn_submit" type="submit" class="btn btn-primary">Submit</button>
                    <button type="button" class="btn btn-danger go-back">Go back</button>
                </div>  
         </div> 
        <div class="span6">
            <div class="control-group">
                <ul id="clock">	
            	   	<li id="sec"></li>
            	   	<li id="hour"></li>
            		<li id="min"></li>
            	</ul>
            </div>
        </div>
    </form>
    <?php endif; ?>
 </div>

<script language="javascript" type="text/javascript" src="/js/full-calendar/fullcalendar.min.js"></script>
<script type="text/javascript" src="/js/jquery-ui-1.8.12.custom.min.js"></script>
<script type="text/javascript" src="/js/jquery.ui.timepicker.js"></script>
<script type="text/javascript" src="/js/jqBootstrapValidation.js"></script>
<link rel="stylesheet" type="text/css" href="/css/jquery-ui-1.8.12.custom.css"/>
<link rel="stylesheet" type="text/css" href="/css/bootstrap-datetimepicker.min.css"/>
<link rel="stylesheet" type="text/css" href="/css/jquery.ui.timepicker.css"/>
<script type="text/javascript" src="/js/bootstrap-modal.js"></script>
<script type="text/javascript" src="/js/bootstrap-modalmanager.js"></script>
<link rel="stylesheet" type="text/css" href="/css/bootstrap-modal.css"/>
<script type="text/javascript" src="/js/file-validator.js"></script>
<link rel="stylesheet" type="text/css" href="/css/file-validator.css"/>

<script type="text/javascript">

    function palert(msg){
        $('.alert-error').remove();
        $('.page-header').after('<div class="alert alert-error">'+msg+'</div>');
        $('form#timing_form button[type=submit]').prop('disabled', false);
        return;
    }
    $('#work').on('change', function() {

        if(this.value == 1)
        {
            $('.train').show();
        }
        else
        {
            $('.train').hide();
        }

    });
    
    $(document).ready(function() {
        initSearchOption('staff_id','search_staff');
        $('.train').hide();
        $( '.date' ).datepicker({ dateFormat: "dd/mm/yy" });
        $('.go-back').click(function(e){

            window.location.href = $('#back_url').val();
            return false;
        });

        var calendar = $('#calendar').fullCalendar({
            header: {
                left: 'prev,next today',
                center: 'title',
                right: 'month'
            },
            eventClick: function (calEvent, jsEvent, view) {
                $modal = $('#modal');
                var obj = calEvent.infos;
                if (obj.length>0) {
                    var html = '';
                    for (var i = 0; i < obj.length; i++) {
                        html += '<li><strong class="product-name">'+obj[i]['product_name']+'</strong><ul>';
                        html += '<li class="imei">'+obj[i]['imei']+'</li>';
                        html += '<li class="customer_name">'+obj[i]['customer_name']+'</li>';
                        html += '<li class="customer_phone">'+obj[i]['customer_phone']+'</li>';
                        html += '<li class="customer_address">'+obj[i]['customer_address']+'</li></ul></li>';
                    }
                    $modal.find('.infos-body').html(html);
                    $modal.modal();
                }
            },
            selectable: true,
            selectHelper: true,
            events: "/time/event?id=<?php
                    if (isset($this->timing) and $this->timing->staff_id!=$this->user_id)
                        echo $this->timing->staff_id;
                    else
                        echo $this->user_id;
                ?>"
        });

        $('form#timing_form').bind('submit',function(e){
            $('form#timing_form button[type=submit]').prop('disabled', true);
        });

        setInterval( function() {
            var seconds = new Date().getSeconds();
            var sdegree = seconds * 6;
            var srotate = "rotate(" + sdegree + "deg)";

            $("#sec").css({"-moz-transform" : srotate, "-webkit-transform" : srotate});

        }, 1000 );


        setInterval( function() {
            var hours = new Date().getHours();
            var mins = new Date().getMinutes();
            var hdegree = hours * 30 + (mins / 2);
            var hrotate = "rotate(" + hdegree + "deg)";

            $("#hour").css({"-moz-transform" : hrotate, "-webkit-transform" : hrotate});

        }, 1000 );


        setInterval( function() {
            var mins = new Date().getMinutes();
            var mdegree = mins * 6;
            var mrotate = "rotate(" + mdegree + "deg)";

            $("#min").css({"-moz-transform" : mrotate, "-webkit-transform" : mrotate});

        }, 1000 );

        $('form#time_form').bind('submit',function(e){
            $('form#time_form button[type=submit]').prop('disabled', true);
        });


    });

</script>