    <table class="table table-bordered">
        <thead>
        <tr>
            <th style="text-align: center;" width="5%">No.</th>
            <th style="text-align: center;" width="55%">Type</th>
            <th style="text-align: center;" width="8%">Status</th>
            <th style="text-align: center;" width="7%">Action</th>
        </tr>
        </thead>
        <tbody>

        <?php
        $count = 0;
        if (isset($this->list) and $this->list)
            foreach ($this->list as $k => $item):
                ?>
                <tr>
                    <td style="text-align: center;"><?php echo $k + 1; ?></td>
                    <td><?php echo isset($item['id']) ? $item['name'] : ''; ?></td>
                    <td style="text-align: center;"><?php echo isset($item['id']) ? EnableDisplay($item['enable']) : ''; ?></td>
                    <td style="text-align: center;">
                        <a title="Edit" href="/trainer/knowledge-base-type-edit?id=<?php echo $item['id']; ?>"
                           style="padding-right: 0.5em;"><i class="icon-edit"></i></a>&nbsp;
                        <a title="Delete" href="/trainer/knowledge-base-type-delete?id=<?php echo $item['id']; ?>" onclick="return confirm('Are you sure to remove?');"><i class="icon-trash"></i></a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>

    <div class="paging">
        <?php echo $this->paging($this->total, $this->limit, $this->offset, $this->url);?>
    </div>
